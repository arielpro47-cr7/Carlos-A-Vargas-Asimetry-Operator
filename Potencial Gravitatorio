import sympy as sp
from vargas_operator import aplicar_operador_vargas

r = sp.symbols('r', positive=True)
phi = -1/r # Potencial de Newton

asimetria, derivada = aplicar_operador_vargas(phi, r)

print("--- ANALISIS GRAVITATORIO DE VARGAS ---")
print(f"Potencial: {phi}")
print(f"Fuerza Clásica (Im): {derivada}")
print(f"Términos de Asimetría Ocultos (Real): {asimetria}")
# Esto mostrará términos como b/r^3 que explican anomalías orbitales
